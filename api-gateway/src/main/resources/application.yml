spring:
  application:
    name: api-gateway

server:
  port: 9090

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always