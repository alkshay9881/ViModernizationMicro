server:
  port: 9090

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:

          # -------- login SERVICE --------
        - id: login-service
          uri: lb://LOGIN-SERVICE
          predicates:
            - Path=/api/auth/**
#          filters:
#            - name: CircuitBreaker
#              args:
#                name: loginServiceCB
#                fallbackUri: forward:/fallback/login

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always



